import { View } from "react-native";

// This is a dummy screen for the sign out tab
// The actual sign out logic is handled by the tab press listener in _layout.tsx
export default function SignOutScreen() {
  return <View />;
}
